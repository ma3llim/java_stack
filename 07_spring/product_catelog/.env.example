# Example environment variables for Product Catalog (copy to .env and fill values)

JWT_SECRET=REPLACE_WITH_SECURE_RANDOM_BASE64
JWT_ISSUER=product-service
ACCESS_TTL_SECONDS=3600
REFRESH_TTL_SECONDS=604800
DB_NAME=products
DB_USERNAME=root
DB_PASSWORD=REPLACE_DB_PASSWORD